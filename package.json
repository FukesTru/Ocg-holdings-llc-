{
  "name": "ocg-financial-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for OCG Financial. The pages are generated by the Python scripts in build/; this manifest exists only so Vercel installs dependencies for the serverless function in api/.",
  "dependencies": {
    "rss-parser": "^3.13.0"
  }
}
